<h2>New Article</h2>

<%= form_for @article do | t | %>
	<p>
		<%= t.label :title %>
		<%= t.text_field :title %>
	</p>

	<p>
		<%= t.label :description %>
		<%= t.text_area :description %>
	</p>
	<p>
		<%= t.submit %>
	</p>

<% end %>

